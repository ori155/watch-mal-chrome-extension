{
  "name": "gogoamine-mal-plugin",
  "version": "1.0.0",
  "description": "A simple chrome extension which enable you to go to the anime in gogoanime via myanimelist website.",
  "main": "index.js",
  "scripts": {
    "start": "./node_modules/.bin/babel src --out-dir lib --source-maps inline --watch & ./node_modules/.bin/chrome-extension run ./lib/manifest.json -o ./build/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "chrome-extension",
    "mal",
    "gogoanime"
  ],
  "author": "Ori",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.14.0",
    "babel-preset-react": "^6.11.1",
    "chrome-extension-scripts": "^0.9.2",
    "webpack": "^1.13.2"
  },
  "dependencies": {
    "react": "^15.3.1",
    "react-bootstrap": "^0.30.3",
    "react-dom": "^15.3.1"
  }
}
